#### Dấu phép toán số học trong LaTeX
- `\times`: Tạo dấu $\times$ (như trong nhân số học).
Ví dụ: `a \times b` → $a \times b$
- `\cdot`: Tạo dấu $\cdot$ (chấm giữa, thường dùng cho tích vô hướng).
Ví dụ: `a \cdot b` → $a \cdot b$

